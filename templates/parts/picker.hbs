{{!--
  The click-to-choose drawer for one slot, rendered on demand by
  scripts/loadout/controller.mjs#openPicker into the loadout's .lo-picker-host.
--}}
<div class="lo-picker" role="dialog" aria-label="{{label}}" data-lo-picker="{{key}}">
  <header class="lo-picker-head">
    <img class="lo-picker-icon" src="{{placeholder}}" alt="" draggable="false">
    <h3 class="lo-picker-title">{{label}}</h3>
    <button type="button" class="lo-icon-button" data-lo-action="close-picker"
            aria-label="{{localize "sogrom-simple-dnd5e-loadout.picker.close"}}">
      <i class="fa-solid fa-xmark" inert></i>
    </button>
  </header>

  {{#if candidates.length}}
  <input type="search" class="lo-picker-search" placeholder="{{searchLabel}}" aria-label="{{searchLabel}}"
         autocomplete="off" spellcheck="false">
  <ul class="lo-picker-list">
    {{#each candidates}}
    <li data-lo-name="{{searchName}}">
      <button type="button" class="lo-picker-item rarity-{{rarity}} {{#if wornIn}}is-worn{{/if}}" data-lo-choose="{{id}}"
              data-tooltip="{{tooltip}}" data-tooltip-class="{{tooltipClass}}" data-tooltip-direction="LEFT">
        <img src="{{img}}" alt="" draggable="false">
        <span class="lo-picker-name">{{name}}</span>
        {{#if wornIn}}<span class="lo-picker-meta">{{wornIn}}</span>{{/if}}
        {{#if canAttune}}<i class="lo-picker-attune fa-{{#if attuned}}solid{{else}}regular{{/if}} fa-sun" inert></i>{{/if}}
      </button>
    </li>
    {{/each}}
  </ul>
  <p class="lo-picker-empty" hidden>{{localize "sogrom-simple-dnd5e-loadout.picker.noMatch"}}</p>
  {{else}}
  <p class="lo-picker-empty">{{noneLabel}}</p>
  {{/if}}
</div>
